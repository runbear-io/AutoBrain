Task 10 retained QA evidence (redacted, fail-closed)
===================================================

Release binding
---------------
- Current release version: 0.1.1.
- Current candidate commit: 036714b91df82d1eb538d39d8251f86ae9f55a4e.
- Runtime evidence status: UNBOUND_CURRENT_RELEASE.
- Reason: the retained QA record names an installed
  `autobrain-0.1.0-py3-none-any.whl` and does not record a source commit.
- Therefore the retained report screenshots and runtime observations are not
  evidence that release 0.1.1 or the current candidate commit passed runtime
  QA. Their bytes are preserved as historical observations only; they were not
  regenerated, relabeled, or represented as current-release evidence.

Historical synthetic observations
---------------------------------
- Fake-MCP mode used a local JSON fixture guarded by both required environment
  variables. The fixture was recorded as synthetic Slack and Notion data with
  safe HTTPS source URLs, three deterministic candidates, and no oracle,
  reference, raw-corpus, or secret material.
- The retained record says two fixture runs executed Slack and Notion crawling
  and all three candidates, with distinct immutable run IDs and stable output
  after run-ID normalization.
- The retained record says installed Chrome captured the three report images
  at 375, 768, and 1280 pixels, with no horizontal overflow, console errors,
  external requests, scripts, inline handlers, secret patterns, or
  oracle/reference markers.
- Those observations remain useful for historical image and text safety review,
  but current release provenance is NOT_ESTABLISHED.

Release evidence policy
-----------------------
- The exact six retained files are closed by SHA-256 in `manifest.json`.
- The manifest self-hash is SHA-256 over its exact UTF-8 bytes after replacing
  the single `hashes.manifest.json` value with 64 ASCII zeroes.
- A retained runtime artifact may claim current-release verification only when
  it records both the current release version and source commit. Missing or
  mismatched provenance is represented by a typed fail-closed status.
- No corpus, token, raw provider output, or secret value is retained.

Retained allowlist
------------------
- `comparison.json`
- `manifest.json`
- `screenshots/report-1280.png`
- `screenshots/report-375.png`
- `screenshots/report-768.png`
- `task-10-final-qa.txt`
